<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WHEELDATA</title>
    <link rel="icon"  href="./favicon.png" type="image/x-icon">
    <link rel="stylesheet" href="./css/style.css">

    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;700&display=swap');

        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }

        .studentinfo {
            max-width: 600px;
            margin: 20px auto;
            background-color: #ffffff;
            border-radius: 15px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            transition: box-shadow 0.3s ease-in-out;
        }

        .studentinfo:hover {
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.2);
        }

        .header {
            text-align: center;
            padding: 20px 0;
            background-color: #3498db;
            color: #ffffff;
            border-top-left-radius: 15px;
            border-top-right-radius: 15px;
            transition: background-color 0.3s ease-in-out, color 0.3s ease-in-out;
        }

        .header:hover {
            background-color: #2a75ad;
        }

        .content {
            padding: 20px;
        }

        .row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid #ddd;
            padding: 10px 0;
            transition: background-color 0.3s ease-in-out;
        }

        .row:hover {
            background-color: #f2f2f2;
        }

        label {
            font-size: 18px;
            margin: 0;
            padding: 5px;
            color: #3498db;
        }

        .row p {
            margin: 0;
            font-size: 18px;
            color: #333333;
        }

        #pending {
            background-color: #27ae60;
            color: #ffffff;
            padding: 5px 10px;
            border-radius: 5px;
            transition: background-color 0.3s ease-in-out, color 0.3s ease-in-out;
        }

        #pending-label:hover {
            background-color: #218c54;
        }
    </style>
    <script>
          

 function formatDateISOToDDMMYYYY() {
            var isoDate = "{{data.AddmissionDate}}"; // Assuming data.AddmissionDate is in ISO format
            var date = new Date(isoDate);
            var day = date.getDate();
            var month = date.getMonth() + 1; // Months are zero-based
            var year = date.getFullYear();

            // Add leading zero if day or month is a single digit
            day = day < 10 ? '0' + day : day;
            month = month < 10 ? '0' + month : month;

            var formattedDate = day + '-' + month + '-' + year;

            // Set the formatted date to the content of the paragraph element
            document.getElementById("formattedDate").textContent = formattedDate;
        }
    </script>
</head>

<body onload="formatDateISOToDDMMYYYY()">

    <div class="studentinfo">
        <center>
            <h2>
                <p id="studentname">{{data.Name}} </p>
            </h2>
        </center>
        <div class="content">
            <div class="row">
                <label for="">Student id<p>{{data.ID}} </p></label>

                <label for="">Mobile No<p> {{data.MobNo}}</p></label>

                <label for="">LLR TYPE<P>{{data.LLRType}}</p></label>
            </div>
            <div class="row">
                <label for="Licence Number">Licence Number <p>{{data.LicenceNo}}</p></label>
            </div>

            <div class="row">
                <label for="">ADD Date <p id="formattedDate"></p></label>

                <label for="">Admission Type <p>{{data.Type}}</p></label>

                <label for="">MDL STATUS<P>{{data.MDLStatus}}</p></label>
            </div>

            <div class="row">
                <label for="">Total Fee <p>{{data.Total}}</p></label>

                <label for="">Deposite Fee <p> {{data.Deposite}} </p></label>

                <label for="" id="pending" style="background-color: greenyellow; color: black;">Pending Fee:-<P>
                        {{data.Pending}}</p></label>
            </div>
        </div>

    </div>
</body>

</html>